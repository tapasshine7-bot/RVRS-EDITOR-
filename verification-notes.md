# REVRSE EDITOR Verification Notes

- **2026-08-22 — Cloudflare Workers migration package:** Added an isolated `cloudflare/` Worker source package without replacing the Node/Express release. `pnpm cf:check` passed, `pnpm cf:build` produced the Static Assets bundle, and the complete Vitest suite passed **36 tests across 9 files**.
- **2026-08-22 — Local Worker smoke test:** The Worker served the root route and an arbitrary SPA route with HTTP 200. With no D1/R2/Access bindings present locally, `/api/projects` returned an explicit HTTP 503 configuration response. This verifies the safe failure path: local editor assets can load, but account/community persistence is not silently emulated or made public.
- **2026-08-22 — Cloudflare security boundary:** The Worker source validates signed Cloudflare Access JWTs against the configured team JWKS, issuer, audience, expiry, and server-only owner email before granting account or moderation actions. Its D1 migration stores metadata/project JSON only; binary shared media is accepted only after rights attestation and is written to R2. No Cloudflare resource, D1 migration, R2 bucket, Worker secret, domain, or deployment has been created or changed during this source-validation phase.

- **2026-08-22:** The dashboard rendered at the managed preview URL with REVRSE EDITOR branding, canvas starters, local recent projects, original editable templates, privacy guidance, and explicit account-sync fallback messaging.
- **2026-08-22:** The original editable motion-study workspace opened successfully. The preview, media bin, transform inspector, multitrack timeline, command controls, local autosave indicator, import/export controls, and optional **Save copy** action were visible and interactive.
- **Constraint confirmed:** No audio import, playback, or audio timeline control is present in the editor workflow.
- **Accessibility implementation:** Interactive controls use native elements or explicit button roles, icon-only controls carry labels, the mobile workspace exposes an in-context close control, reduced-motion preferences are respected, and the final global `:focus-visible` treatment gives keyboard users a high-contrast cyan focus outline. End-to-end keyboard and mobile-panel verification remains in the release checklist.
- **Fallback implementation:** Corrupt browser-local state normalizes safely; invalid JSON backups show a clear recovery message; unreadable SRT files and SRT files without timed captions return clear errors; locked caption tracks explain how to continue; and the unauthenticated project-sync endpoint returns the expected 401 while the interface continues in browser-local mode. Remaining user-interface error-path tests remain in the release checklist.
- **Hands-on workspace check:** In the rendered browser workspace, Tab moved into the editor controls with a visible focus treatment, the command palette opened through the keyboard shortcut, and selecting a timeline clip revealed its transform, appearance, timing, and keyframe inspector controls.
- **Observed caption fallback:** A deliberately malformed SRT file was dispatched through the app’s real caption file input. The editor displayed: “No valid timed captions were found in that SRT file.”
- **Observed project-backup fallback:** A deliberately malformed REVRSE project JSON file was dispatched through the app’s real project file input. The editor displayed: “This project file could not be read. Choose a valid REVRSE project JSON file.”
- **Caption-track protection setup:** The rendered timeline was used to lock the “Titles & captions” track; the control changed from “Lock” to “Unlock,” confirming the track-lock state is active for the final guarded-caption insertion check.
- **Observed locked-caption fallback:** With the captions track locked, the command palette’s “Add caption” action was executed in the live workspace. The editor prevented the insertion and displayed: “Unlock or add a text track before adding text.”
- **Observed mobile panel opening:** The mobile “Captions” panel control was exercised against the running component. After React applied the state change, the panel rendered its “Captions” title and an accessible “Close panel” control.
- **Observed mobile panel closing:** The rendered “Close panel” control was activated. After the state update, the mobile captions panel was removed as expected.
- **Observed unreadable-project fallback:** A file whose browser read operation was deliberately rejected was routed through the real project import control. The editor displayed: “This project file could not be read. Choose a valid REVRSE project JSON file.”
- **Observed account-sync fallback:** Activating “Save copy” without a signed-in session displayed: “Account sync is optional. Your project is autosaved in this browser and can be exported as a JSON backup.” The editable workspace remained usable.
- **Account error branch review:** The authenticated save mutation has a dedicated unavailable-sync feedback branch that states: “Account sync is unavailable. Your browser-local autosave and JSON backup remain intact.” The public dashboard also renders an account-sync-unavailable panel if an authenticated project-list request fails. The authenticated error branch requires a signed-in account to invoke end-to-end; the no-session fallback was exercised above.
- **Runtime account baseline:** In the unauthenticated preview, the account query resolved with no user and both account-project queries remained inactive. A transient in-memory query-state test can therefore be used to validate the error presentation without writing account data or changing browser-local project data.
- **Observed account-sync unavailable UI:** In a transient in-memory preview account state, the account-project request reached its expected unauthenticated error response. The dashboard rendered: “Account sync is temporarily unavailable. Browser-local autosave and JSON backups continue to work.” No account or project data was written during this verification.
- **Local-editing outage test status:** While the transient account-project request was in an error state, a synthetic DOM-only project-name input was correctly ignored by React and did not alter local storage. The remaining check will use the real input interaction so the autosave signal is genuine.
- **Signed-in outage limitation:** A real authenticated outage cannot be initiated without asking the user to sign in, which is intentionally outside this release verification. Instead, an in-memory preview account caused the existing project-list procedure to return its real unauthenticated response and rendered the actual fallback message: “Account sync is temporarily unavailable. Browser-local autosave and JSON backups continue to work.” This simulation wrote no account or project data.
- **Browser-local recovery evidence:** The unauthorized request invoked the app’s login redirect, after which the editor was reopened without signing in. The existing local project remained available. A real project-title update to “REVRSE Motion Study — local autosave verified” persisted in both `revrse-editor-local-project` and `revrse-editor-recent-projects`, confirming the browser-local edit and autosave recovery path remains usable without account sync.
- **Non-persistent outage test setup:** For the final UI verification, browser `fetch` was temporarily intercepted only for the account-project list operation to return a 503-shaped tRPC error. A normal REVRSE grid toggle was then used to capture the mounted editor state; this confirms the editor remains interactive while the transient test harness is prepared.
- **Same-session local autosave during outage:** In the same transient session, the account-project query entered an error state while a real project-title edit was made through the rendered REVRSE EDITOR input. The title became “REVRSE Motion Study — sync outage local edit” and was confirmed in browser-local storage while the account query still reported `error`. The test interceptor and preview account state were then removed; no account or server project data was written.
- **Post-foundation 4K dashboard check:** Desktop and phone-sized dashboard views rendered the landscape **3840 × 2160**, vertical **2160 × 3840**, and square **2160 × 2160** canvas starters. The interface states that live preview is deliberately scaled to the active device while the project retains its full selected dimensions.
- **Licensed-audio timeline check:** The browser-local demo opened into the responsive editor shell with the restored **Licensed sound** track alongside video, overlays, titles/captions, and adjustment tracks. No audio started automatically during the check.
- **Panel-control discovery:** The rendered workspace exposes dedicated **Captions**, **Sounds**, and **Studio** panel controls. The next browser pass will exercise their account-gated empty states and non-autoplay use controls.
- **Desktop command palette check:** The workspace command palette opened and exposed its existing edit, templates, assistant, and export actions. Creator Studio and licensed-sound panels remain contextual panel controls rather than command-palette entries; their mobile-panel path remains the appropriate responsive validation target.
- **Development reload observation:** A code hot reload returned the preview to the dashboard while retaining the browser-local recent project list. Subsequent UI checks reopen an existing local project rather than relying on transient workspace state.
- **Custom-canvas UI check:** Project settings rendered all 4K presets plus the new labelled width and height inputs, apply action, 160–7680 px bounds, and a clear device/browser/source-dependent performance disclaimer.
- **Custom-canvas boundary test:** Entering 9000 × 100 and applying the settings produced a success notice and re-rendered the inputs at the expected safe bounds of **7680 × 160**. The canvas ratio switched to Custom without a workspace crash.
- **4K restoration and panel discovery:** The validation project was restored to the 3840 × 2160 4K landscape preset. Live control inspection confirmed dedicated **Captions**, **Templates**, **Dialogue**, **Sounds**, and **Studio** panel controls are rendered in the desktop workspace.
- **4K final state:** Selecting the 4K landscape preset restored the local project to **3840 × 2160** and re-synced the custom-dimension fields accordingly. The workspace remained stable while the Movie Dialogue panel was opened for the next validation pass.
- **Movie Dialogue validation:** The workspace rendered its no-download/no-rehosting notice, optional lawful-source record, rights confirmation, and editable-caption field. A neutral, original test caption with an example licensed-source URL was inserted only after rights confirmation; it appeared as an editable caption row, selected clip, preview overlay, and timeline item.
- **Saved-caption recovery and search validation:** After reopening the browser-local project, two saved caption rows appeared in Movie Dialogue Workspace even while the editable-caption draft retained unrelated text. Caption search is now independent of the draft, preventing saved rows from being hidden by the compose field.
- **Caption jump and update validation:** Selecting a saved caption row moved the playhead to its timeline position, selected the matching text clip, and loaded its text into the editable caption field. Saving an edited neutral local caption updated the transcript row, matching timeline clip, media-bin label, preview overlay, and inspector selection; the interface displayed “Editable caption updated.”
- **Licensed Sound Library validation:** The live panel displayed its rights-only policy, explicit exclusion of copied commercial song, film-dialogue, and meme catalogues, supported audio formats and 12 MB maximum, ownership/rights attestation, account-gated publish message, and empty-library feedback. It offered no autoplay control and did not start audio during validation. No test sound was uploaded or published.
- **Creator Studio validation:** The live Studio panel displayed distinct expandable forms for an original editable template and a licensed reusable video, including required rights attestations, legal file guidance, account-gated publishing feedback, and empty shared-resource states. No template, video, rating, or review was fabricated during validation.
- **Phone-sized custom-canvas settings validation:** In an emulated 375 × 812 viewport, the custom-canvas dialog exposed width and height inputs fully within the viewport at 134 px each, an in-bounds Apply control, visible device/browser performance guidance, and no horizontal overflow. The custom-dimension flow therefore remains usable on a phone-sized layout.
- **Creator Studio dashboard entry:** Desktop preview confirms a dedicated Creator Studio card in the project dashboard, with explicit original-template and reusable-media guidance and a direct “Open Creator Studio” action. It is visually separated from local starter templates and account-saved project structure.
- **Shared-library regression coverage:** The automated suite now includes a schema-level regression confirming that shared sounds retain only storage metadata and rights attestation (never media bytes), sound favourites are user-and-resource scoped, and authentic review targets include sound resources. The suite passed with 11 tests across 3 files.
- **Phone-sized media-workflow validation:** In an emulated 375 × 812 viewport, the responsive panel controls opened **Movie Dialogue Workspace**, **Licensed Sound Library**, and the **Creator Studio** publishing forms independently. Each panel retained its accessible close control and produced no horizontal overflow. No media, template, favourite, rating, or review data was created during this inspection.
- **Authenticated shared-resource contract coverage:** Isolated typed procedures verified that an attested reusable video and lawful sound are routed to object storage and saved by metadata key, while template and sound save actions are forwarded only for a signed-in user. Validation tests reject missing sound rights attestation, unsupported audio types, invalid star values, and unsupported review resource types. These checks created no real media, favourites, ratings, or reviews. The final suite passed **18 tests across 5 files**.
- **Dashboard Creator Studio verification:** Desktop review confirms the Creator Studio dashboard section now has an explicit empty state for shared templates, a direct Studio entry point, and dedicated cards that reveal per-template **Use in edit** and signed-in **Save/Saved** controls whenever lawful shared templates exist. This complements the expanded Studio workspace without inventing template records for preview.
- **Creator productivity dashboard validation:** The browser-local dashboard now provides a title search, canvas-ratio filter, and sort control for recent projects, alongside local-project, total-timeline, and 4K-canvas summaries. A focused project-finder test verifies filtering, sorting without state mutation, and full-4K statistics. Desktop and 375 × 812 screenshots confirm that the compact controls, local-project card, new-project action, Creator Studio entry, and non-network local-first guidance remain readable without horizontal overflow. The full suite passed **21 tests across 6 files** with TypeScript validation.
- **Optional Jamendo discovery fallback validation:** In the live Sound Library, the collapsed provider panel opened to a clear metadata-only policy, an official developer-portal link, and a no-credential setup state. It returned no provider tracks, showed no audio player or autoplay control, and left the existing lawful creator-upload workflow unchanged. Automated coverage confirms that metadata search rejects while the server-side credential is absent; the suite then passed **24 tests across 6 files**.
- **Enhanced lawful Sound Library validation:** In the live editor, the redesigned Sound Library rendered its creator-owned/public-domain/royalty-free/licensed policy, title/creator/mood/genre/licence search, category and duration filters, an accurate zero-track state, and the Jamendo no-credential fallback. The expanded signed-in publishing form exposes source/licence URL, credit, mood, and licence-type fields with an explicit distribution-rights attestation. No audio was uploaded, downloaded, played, published, or assigned to the timeline; no shared tracks, ratings, reviews, reports, or favourites were created during this check.
- **In-editor-only music-use validation:** The live Sound Library now explicitly states that permitted music is added to the current project and that REVRSE EDITOR offers **no standalone audio downloads**. The rendered Jamendo section remains metadata-only and no-credential-safe, and the visible controls contained search, filtering, publication, and project-use pathways only. No music was downloaded, previewed, played, uploaded, published, or added to the timeline during this verification. TypeScript validation and the full Vitest suite passed afterward with **25 tests across 6 files**.
- **Export-preparation validation:** In the live demo editor, the Export dialog identified the editable JSON/SRT package, reported browser MediaRecorder capability as WebM available and MP4 not reported, and clearly stated that capability detection does not create a video render. Selecting **Prepare outputs** reached the visible 100% complete state without playback, media encoding, or downloads. Selecting **Cancel** returned the dialog to the 0% ready state, keeping only the existing JSON/SRT download actions available. No output file was downloaded during this check.
- **Final mobile layout check:** A full-page 375 × 812 capture kept the REVRSE EDITOR project launcher, canvas options (including 4K/custom), local-project finder, templates, Creator Studio entry, account-sync notice, and workspace principles in a single-column layout with readable controls and no horizontal clipping. The in-editor Sound Library and export-dialog behaviors were separately verified in the live editor without media playback or downloads.
- **Release regression check:** `pnpm check`, `pnpm test`, and `git diff --check` all completed successfully. The final suite passed **27 tests across 7 files**, including the deterministic browser export-preparation capability/progress coverage. No sample creator tracks, community reviews, reports, or moderation records were created for this validation.

- **Sidebar layout baseline:** The project launcher remained clean and readable after the workspace-sidebar implementation. At 1280 × 720, REVRSE EDITOR branding, starter canvases, import/new-project actions, and workspace principles rendered without visible overlap. At 375 × 812, the launcher stayed single-column with readable canvas cards, an accessible new-project action, and no horizontal clipping. The responsive workspace sidebar itself is desktop-only by design; the existing compact panel bar remains the phone control pattern. No media, recordings, or community data were created during the screenshot checks.

- **Current terminology and responsive check:** The latest 1280 × 720 and 375 × 812 captures show the REVRSE EDITOR launcher using “Open starter project” rather than visible “demo” wording. Canvas starters remained legible and vertically reachable on the phone viewport. No media was imported, recorded, uploaded, played, or downloaded during this visual check.

- **Sidebar and voice-over regression coverage:** The workspace sidebar’s active-panel state and browser-local clip/track status are now backed by deterministic helpers and exercised in Vitest. Voice-over support detection, denied-microphone messaging, generic start failures, and discard/empty-recording decisions are likewise covered without opening a microphone. The final TypeScript check, whitespace check, and suite passed with **32 tests across 8 files**.

- **Browser-permission limitation:** The automated preview did not request microphone access and did not record voice. In an end-user browser, recording requires an explicit microphone permission grant; unsupported or denied states show a clear fallback to import original or licensed local audio. Recorded blobs remain browser-local object URLs and are never uploaded automatically.

- **Provider-catalogue release decision:** The user elected to add a music set later. Accordingly, no Apollo, Soundstripe, Mubert, Feed.fm, or other external catalogue was activated. The release keeps the verified creator-owned local-audio workflow, first-party empty-state safeguards, and metadata-only Jamendo boundary; future in-editor catalogue preview requires an authorized provider agreement and credentials.
